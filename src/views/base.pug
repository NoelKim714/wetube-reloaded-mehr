doctype html 
html(lang="ko")
    head
        title #{pageTitle} | Wetube
        link(link rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
        header
            h1=pageTitle
            nav
                ul 
                    li 
                        a(href="/") Home
                if !req.session.loggedIn //pug랑 공유 안해서 사이트 안나올 것
                    li
                        a(href="/join") Join
                    li 
                        a(href="/login") Login
                li 
                    a(href="/videos/upload") Uplaod Video
                li 
                    a(href="/search") Search
                    

        main  
            block content 
    include partials/footer